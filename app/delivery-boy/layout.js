import { Metadata } from "next";

export const metadata = {
    title: "Delivery | Pumato",
    description: "Pumato delivery dashboard",
};

export default function DeliveryBoyLayout({ children }) {
    return children;
}
